{
  "name": "eleventy-site",
  "version": "1.0.0",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "build:production": "eleventy --pathprefix=/enigmorency",
    "build:css": "sass src/css/styles.scss _site/css/styles.css --style=compressed",
    "dev": "eleventy --serve --watch"
  },
  "devDependencies": {
    "@11ty/eleventy": "3.1.2",
    "sass": "^1.89.2"
  }
}
